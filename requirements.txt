--extra-index-url https://download.pytorch.org/whl/cu121
--extra-index-url https://pypi.nvidia.com

# Core ML and Deep Learning Libraries (Pinned for CUDA 12.1 compatibility per cluster constraints)
torch>=2.1.2+cu121
torchvision>=0.16.2+cu121
numpy>=1.21.0,<2.0.0

# Transformers and Model Libraries
transformers>=4.35.0
accelerate>=0.20.0
peft>=0.5.0
trl>=0.7.0

# Computer Vision and Image Processing
Pillow>=9.0.0

# Data Processing Libraries
pandas>=1.5.0
datasets>=2.14.0
fastparquet>=0.8.0

# Progress and Evaluation
tqdm>=4.64.0
evaluate>=0.4.0
scikit-learn>=1.0.0

# Flash Attention
# Installed to optimize throughput for the Qwen2-VL model on the A100 cluster
flash-attn>=2.3.0

# GPU-Accelerated PCA (RAPIDS array of identical versions to avoid cudf/rmm mismatch)
cudf-cu12==24.12.0
rmm-cu12==24.12.0
cuml-cu12==24.12.0

